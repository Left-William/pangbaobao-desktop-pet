# 胖宝宝独立桌宠：工程验证记录

日期：2026-09-24

版本：0.2.0

定位：可运行的独立桌宠，伸展、转体、踮脚补帧版已接入，整体视觉仍需用户验收。

## 分阶段进度

| 阶段 | 当前结果 | 验收状态 |
| --- | --- | --- |
| P0 现成方案验证 | 固定并检查 VPet 官方仓库 `c150e6f8c97f67662b249d2db27d72c3709e40bd`。其当前 Windows 工程目标为 `net10.0-windows7.0`，本机原有 SDK 最高为 7；GitHub Releases 没有现成程序包，且本机没有 Steam VPet。选择本机可构建的成熟 WPF 桌面框架作为程序底座。 | VPet 自定义素材与气泡未实机跑通；替代方案已实际运行。 |
| P1 单节 24 FPS 样片 | 原 13 帧直接插帧会产生明显残影。依据照片另制 7 个抬手关键姿势，使用 RIFE ncnn Vulkan `rife-v4.6` 对白底图像与 alpha 分别离线插帧，组装为 36 帧抬手与放手循环，保留原版对照。 | 伸展样片已接入并本机显示；快速移动时少数手部纹理仍有变化，等待用户看动态效果。 |
| P2 桌面最小可用版 | 独立 EXE、透明置顶窗口、拖动、缩放、单节播放、整套播放、速度、暂停、位置保存、托盘与退出已实现。 | 本机从发布目录启动并显示窗口；更多显示器/DPI 组合待测。 |
| P3 气泡功能 | 文案增删改及启停、0–100% 概率、判定间隔、冷却、预览、点击关闭、JSON 保存和备份恢复已实现。 | 自动化规则测试通过；本机 100%/5 秒配置下截到中文气泡。 |
| P4 动作扩充 | 六种正式动作加三个原版对照动作，共 122 张播放帧。转体 24 帧、踮脚 20 帧也采用 RIFE 离线补帧。旧动作的紫色残边与原伸展的白边均做了 alpha 清理。原版对照不加入整套广播体操。 | 各动作文件夹和帧数与元数据一致；扩胸、体前屈、侧弯仍低帧率，跨动作外观仍可能跳变。 |
| P5 打包与验收 | 生成 .NET 8 桌面程序发布目录及 ZIP。 | 本机发布目录和 ZIP 解压目录均已启动并显示窗口；其他电脑验证与自包含运行包未完成。 |

## 实际验证

- `dotnet build -c Release`：0 警告、0 错误。构建使用官方 .NET SDK 8.0.425 的临时便携副本。
- `PangBaoBaoPet.SmokeTests`：概率 0%/100%、冷却、单气泡、不同文案优先、隐藏窗口、空文案库、非法数值归一化、损坏配置回退备份均通过。
- Windows NT 10.0.26200 本机安装的 `Microsoft.WindowsDesktop.App 8.0.31` 下，从 0.2.0 发布目录启动 `PangBaoBaoPet.exe`，观察到 585×735 物理像素的可见窗口；运行进程响应正常。更新了截图脚本，使其在高 DPI 屏幕读取 DWM 物理窗口尺寸。
- 使用隔离配置目录，将判定间隔设为 5 秒、命中概率设为 100%，截图确认气泡和人物同时显示。0.2.0 运行截图见 [docs/qa/desktop-bubble.png](docs/qa/desktop-bubble.png)。
- 0.2.0 发布目录含 122 张动作 PNG、动作元数据和图标；无对开发目录的绝对路径引用。设置写入用户配置目录，可单独迁移。
- 清理前扩胸首帧有 326 个可见紫色像素，清理后为 0；原伸展首帧半透明区域有 734 个近白像素，新伸展首帧为 316。数字仅为像素检查，实际边缘仍以视觉验收为准。
- 三节补帧动作的循环预览分别见 [伸展](预览-伸展补帧版-24FPS.webp)、[转体](预览-转体补帧版-24FPS.webp)、[踮脚](预览-踮脚补帧版-24FPS.webp)。转体、踮脚也从发布目录分别启动并截图，窗口可见；原始 QA 截图保留在本机工作目录，未加入仓库。
- 本机 `胖宝宝独立桌宠-0.2.0.zip` 为 5,098,378 字节，附带安装说明与三节动态预览；GitHub Release 使用 ASCII 文件名 `PangBaoBaoPet-0.2.0-win-x64.zip`。本机解压后核对到 122 帧，独立启动程序，窗口可见且进程响应正常。

## 仍需处理

1. **动画质量**：播放器约每 16 ms 检查一次显示时机。伸展由 7 张关键姿势和 RIFE 过渡帧组成 24 FPS 样片，转体和踮脚由旧动作帧经 RIFE 补成 24 FPS；这些不是逐张绘制的新姿势，少数手部、面部与鞋子过渡帧仍有纹理变化。扩胸、体前屈、侧弯仍是 5–6 帧。原图身份、比例、循环接缝和跨动作外观需用户看实际动态效果确认；Live2D 分层源工程尚未交付。
2. **画面边缘**：已清除旧 GIF 的可见紫边并减轻半透明白边；不同动作源图的纹理与画质仍有差异。
3. **分发依赖**：本次采用 framework-dependent .NET 8 发布包，需目标电脑安装 .NET 8 Desktop Runtime。自包含发布所需 runtime pack 无法从 NuGet 服务获取；没有将“本机能运行”等同于干净电脑验收。
4. **交互测试**：已验证窗口显示与气泡渲染，尚未覆盖所有多显示器、DPI、托盘交互和休眠恢复情形。

## 后续视觉制作入口

请先比较右键菜单中的三节补帧版与各自原版，再决定是否为扩胸、体前屈、侧弯增补关键姿势。后三节原帧之间的动作跨度较大，直接光流插帧容易出残影。若需进一步稳定脸、眼镜、衣服和关节比例，可在 Live2D 或同类成熟二维工具中建立可编辑分层模型。程序从 `Assets/actions.json` 和动作文件夹加载逐帧素材，替换素材无需重写气泡与桌面交互逻辑。

## 参考与许可

- [VPet 官方仓库](https://github.com/LorisYounger/VPet)
- [RIFE ncnn Vulkan 官方仓库](https://github.com/nihui/rife-ncnn-vulkan)
- [Microsoft .NET 8 下载](https://dotnet.microsoft.com/download/dotnet/8.0)

发布包仅含本项目代码、用户照片衍生素材和 Microsoft .NET 框架引用；不含 VPet 或 RIFE 的程序、模型和第三方素材。
